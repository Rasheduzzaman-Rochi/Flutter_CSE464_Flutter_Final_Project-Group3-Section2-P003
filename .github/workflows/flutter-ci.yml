name: Flutter CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  flutter-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Show Flutter version
        run: flutter --version

      - name: Install dependencies
        run: flutter pub get
        working-directory: e_commerce_cse464

      - name: Analyze code
        run: flutter analyze
        working-directory: e_commerce_cse464

      - name: Build debug APK
        run: flutter build apk --debug
        working-directory: e_commerce_cse464

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: snapbuy-debug-apk
          path: e_commerce_cse464/build/app/outputs/flutter-apk/app-debug.apk